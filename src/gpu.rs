//use screen_13_window::{Window, WindowError};
use crate::opcodes::*;

struct GPU {
}

impl GPU {
    /*pub fn init() {
        pretty_env_logger::init();

        Window::new()?.run(|frame| {
            frame
                .render_graph
        })
    }
    */
    pub fn write_letter()
}
